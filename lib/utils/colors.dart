import 'package:flutter/material.dart';

class AppColors {
  static const Color darkBlue = Color(0xFF392AAB);   // Azul escuro (Botão)
  static const Color gray = Color(0xFF6E6E6E);       // Cinza
  static const Color lightBlue = Color(0xFF41A3FE);  // Azul (não botões)
  static const Color white = Color(0xFFFFFDFD);      // Branco (texto)
  static const Color orange = Color(0xFFE87A30);     // Laranja (título)
  static const Color black = Color(0xFF121212);      // Preto
  static const Color green = Color(0xFF5DAD6E);      // Verde
  static const Color red = Color(0xFFC0392B);        // Vermelho
}

class AppFonts {
  static const String poppins = 'Poppins';           // Fonte Poppins
}
